{
  "title": "tick-summary",
  "description": "The one object a headless tick returns to its launcher. No \"$schema\" key on purpose: Claude Code 2.1.270 refuses draft 2020-12 by ref, and --json-schema takes inline JSON only, never a path.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "status",
    "summary",
    "refined",
    "dispatched",
    "reflected",
    "awaiting",
    "lock"
  ],
  "properties": {
    "status": {
      "enum": [
        "committed",
        "idle",
        "blocked"
      ]
    },
    "summary": {
      "type": "string",
      "maxLength": 400
    },
    "refined": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dispatched": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "reflected": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "awaiting": {
      "type": "integer",
      "minimum": 0
    },
    "lock": {
      "enum": [
        "released",
        "held",
        "absent"
      ]
    }
  }
}
